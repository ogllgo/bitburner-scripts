import { NS } from "@ns";

export async function main(ns: NS) {
    const doc = eval('document');
    const texts = [ /* texts to add, like HACK or CHA */
        "KMA",
    ];
    const values = [ /* values to add, like 23 or 12 */
        ns.formatNumber(ns.heart.break()),
    ];
    while (true) {
        const number1 = document.getElementById('something');
        const number2 = document.getElementById('something2');
        
    }
}